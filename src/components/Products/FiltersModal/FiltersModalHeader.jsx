import React from 'react';
// Note: This component is no longer needed as Ant Design Modal handles the header
// Keeping for backward compatibility but it's a no-op
const FiltersModalHeader = ({ t, onClose }) => {
    return null;
};

export default FiltersModalHeader;
